name: Execute issue 24 validated source patch

on:
  push:
    branches: [fix/issue-24-anthropic-redirect]
  pull_request:
    branches: [main]

permissions:
  contents: write

jobs:
  patch-test-push:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Apply reviewed steps, test, and push source only
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          git clone "https://x-access-token:${GH_TOKEN}@github.com/${GITHUB_REPOSITORY}.git" repo
          cd repo
          git checkout fix/issue-24-anthropic-redirect

          python3 - <<'PY'
          from pathlib import Path

          lines = Path('.github/workflows/issue-24-patch.yml').read_text(encoding='utf-8').splitlines()
          names = [
              'Add credential-safe redirect policy',
              'Run focused tests',
              'Run race-focused tests',
              'Run full test suite',
          ]

          def extract(name: str) -> str:
              marker = f'      - name: {name}'
              try:
                  index = lines.index(marker) + 1
              except ValueError as exc:
                  raise SystemExit(f'missing reviewed step: {name}') from exc
              while index < len(lines):
                  line = lines[index]
                  if line.startswith('      - name: '):
                      raise SystemExit(f'step {name} has no run command')
                  if line == '        run: |':
                      index += 1
                      body = []
                      while index < len(lines):
                          line = lines[index]
                          if line.startswith('      - name: '):
                              break
                          if line == '':
                              body.append('')
                          elif line.startswith('          '):
                              body.append(line[10:])
                          else:
                              break
                          index += 1
                      if not body:
                          raise SystemExit(f'step {name} has an empty run block')
                      return '\n'.join(body) + '\n'
                  inline = '        run: '
                  if line.startswith(inline):
                      command = line[len(inline):]
                      if not command:
                          raise SystemExit(f'step {name} has an empty inline run command')
                      return command + '\n'
                  index += 1
              raise SystemExit(f'step {name} has no run command')

          for number, name in enumerate(names, start=1):
              Path(f'/tmp/issue-24-step-{number}.sh').write_text(
                  'set -euo pipefail\n' + extract(name), encoding='utf-8'
              )
          PY

          bash /tmp/issue-24-step-1.sh
          bash /tmp/issue-24-step-2.sh
          bash /tmp/issue-24-step-3.sh
          bash /tmp/issue-24-step-4.sh

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add sdk/llm/anthropic/redirect.go sdk/llm/anthropic/client.go sdk/llm/anthropic/redirect_test.go agent_docs/providers.md
          git commit -m "fix(anthropic): block credential-bearing cross-origin redirects"
          git push origin HEAD:fix/issue-24-anthropic-redirect
